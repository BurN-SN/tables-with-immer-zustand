/**
 * Calculator files will:
 *  have public methods that take an object and table type,
 *  use private methods to calculate new values,
 *  mutate object with new values.
 *  return new object to be placed back in state.
 */
